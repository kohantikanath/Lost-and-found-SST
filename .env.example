# Example environment variables for Campus Lost & Found (Next.js)
# Copy this file to `.env.local` and fill in real values.

# Public base URL for server-side fetches in Next.js
# e.g., http://localhost:3000 in development or your deployed URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MongoDB connection string used by src/lib/db.ts
# e.g., mongodb+srv://<user>:<pass>@cluster0.abcd123.mongodb.net/lostfound
MONGODB_URI=

# Cloudinary settings
# Cloud name visible publicly in client requests
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# Upload preset (UNSIGNED) configured in Cloudinary dashboard
# Note: Code currently uses 'campus_lost' preset; update if you change it
# NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=campus_lost

# Optional
# NODE_ENV=development
